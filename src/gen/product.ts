// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v6.33.2
// source: product.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Empty } from "./google/protobuf/empty";

export const protobufPackage = "product";

/** Represents a manufacturable product with its bill of materials (BOM). */
export interface Product {
  id: string;
  name: string;
  bom: BillOfDrySupply[];
}

/** Represents the quantity of a dry supply required per unit of product. */
export interface BillOfDrySupply {
  drySupplyId: string;
  quantityPerUnit: number;
}

export interface CreateProductRequest {
  name: string;
  bom: BillOfDrySupply[];
}

export interface GetProductByIDRequest {
  id: string;
}

export interface GetProductsResponse {
  products: Product[];
}

function createBaseProduct(): Product {
  return { id: "", name: "", bom: [] };
}

export const Product: MessageFns<Product> = {
  encode(message: Product, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.bom) {
      BillOfDrySupply.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Product {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProduct();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bom.push(BillOfDrySupply.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Product {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      bom: globalThis.Array.isArray(object?.bom) ? object.bom.map((e: any) => BillOfDrySupply.fromJSON(e)) : [],
    };
  },

  toJSON(message: Product): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.bom?.length) {
      obj.bom = message.bom.map((e) => BillOfDrySupply.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Product>, I>>(base?: I): Product {
    return Product.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Product>, I>>(object: I): Product {
    const message = createBaseProduct();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.bom = object.bom?.map((e) => BillOfDrySupply.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBillOfDrySupply(): BillOfDrySupply {
  return { drySupplyId: "", quantityPerUnit: 0 };
}

export const BillOfDrySupply: MessageFns<BillOfDrySupply> = {
  encode(message: BillOfDrySupply, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.drySupplyId !== "") {
      writer.uint32(10).string(message.drySupplyId);
    }
    if (message.quantityPerUnit !== 0) {
      writer.uint32(16).uint64(message.quantityPerUnit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BillOfDrySupply {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBillOfDrySupply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.drySupplyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quantityPerUnit = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BillOfDrySupply {
    return {
      drySupplyId: isSet(object.drySupplyId)
        ? globalThis.String(object.drySupplyId)
        : isSet(object.dry_supply_id)
        ? globalThis.String(object.dry_supply_id)
        : "",
      quantityPerUnit: isSet(object.quantityPerUnit)
        ? globalThis.Number(object.quantityPerUnit)
        : isSet(object.quantity_per_unit)
        ? globalThis.Number(object.quantity_per_unit)
        : 0,
    };
  },

  toJSON(message: BillOfDrySupply): unknown {
    const obj: any = {};
    if (message.drySupplyId !== "") {
      obj.drySupplyId = message.drySupplyId;
    }
    if (message.quantityPerUnit !== 0) {
      obj.quantityPerUnit = Math.round(message.quantityPerUnit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BillOfDrySupply>, I>>(base?: I): BillOfDrySupply {
    return BillOfDrySupply.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BillOfDrySupply>, I>>(object: I): BillOfDrySupply {
    const message = createBaseBillOfDrySupply();
    message.drySupplyId = object.drySupplyId ?? "";
    message.quantityPerUnit = object.quantityPerUnit ?? 0;
    return message;
  },
};

function createBaseCreateProductRequest(): CreateProductRequest {
  return { name: "", bom: [] };
}

export const CreateProductRequest: MessageFns<CreateProductRequest> = {
  encode(message: CreateProductRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.bom) {
      BillOfDrySupply.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bom.push(BillOfDrySupply.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      bom: globalThis.Array.isArray(object?.bom) ? object.bom.map((e: any) => BillOfDrySupply.fromJSON(e)) : [],
    };
  },

  toJSON(message: CreateProductRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.bom?.length) {
      obj.bom = message.bom.map((e) => BillOfDrySupply.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductRequest>, I>>(base?: I): CreateProductRequest {
    return CreateProductRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductRequest>, I>>(object: I): CreateProductRequest {
    const message = createBaseCreateProductRequest();
    message.name = object.name ?? "";
    message.bom = object.bom?.map((e) => BillOfDrySupply.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetProductByIDRequest(): GetProductByIDRequest {
  return { id: "" };
}

export const GetProductByIDRequest: MessageFns<GetProductByIDRequest> = {
  encode(message: GetProductByIDRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProductByIDRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProductByIDRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProductByIDRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: GetProductByIDRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProductByIDRequest>, I>>(base?: I): GetProductByIDRequest {
    return GetProductByIDRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProductByIDRequest>, I>>(object: I): GetProductByIDRequest {
    const message = createBaseGetProductByIDRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseGetProductsResponse(): GetProductsResponse {
  return { products: [] };
}

export const GetProductsResponse: MessageFns<GetProductsResponse> = {
  encode(message: GetProductsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.products) {
      Product.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProductsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProductsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.products.push(Product.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProductsResponse {
    return {
      products: globalThis.Array.isArray(object?.products) ? object.products.map((e: any) => Product.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetProductsResponse): unknown {
    const obj: any = {};
    if (message.products?.length) {
      obj.products = message.products.map((e) => Product.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProductsResponse>, I>>(base?: I): GetProductsResponse {
    return GetProductsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProductsResponse>, I>>(object: I): GetProductsResponse {
    const message = createBaseGetProductsResponse();
    message.products = object.products?.map((e) => Product.fromPartial(e)) || [];
    return message;
  },
};

export type ProductServiceService = typeof ProductServiceService;
export const ProductServiceService = {
  /** Creates a product. Returns OK or gRPC error. */
  createProduct: {
    path: "/product.ProductService/CreateProduct",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: CreateProductRequest): Buffer => Buffer.from(CreateProductRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateProductRequest => CreateProductRequest.decode(value),
    responseSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    responseDeserialize: (value: Buffer): Empty => Empty.decode(value),
  },
  /** Returns a single product by ID. */
  getProductById: {
    path: "/product.ProductService/GetProductByID",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetProductByIDRequest): Buffer =>
      Buffer.from(GetProductByIDRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetProductByIDRequest => GetProductByIDRequest.decode(value),
    responseSerialize: (value: Product): Buffer => Buffer.from(Product.encode(value).finish()),
    responseDeserialize: (value: Buffer): Product => Product.decode(value),
  },
  /** Returns all products. */
  getProducts: {
    path: "/product.ProductService/GetProducts",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    requestDeserialize: (value: Buffer): Empty => Empty.decode(value),
    responseSerialize: (value: GetProductsResponse): Buffer => Buffer.from(GetProductsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetProductsResponse => GetProductsResponse.decode(value),
  },
} as const;

export interface ProductServiceServer extends UntypedServiceImplementation {
  /** Creates a product. Returns OK or gRPC error. */
  createProduct: handleUnaryCall<CreateProductRequest, Empty>;
  /** Returns a single product by ID. */
  getProductById: handleUnaryCall<GetProductByIDRequest, Product>;
  /** Returns all products. */
  getProducts: handleUnaryCall<Empty, GetProductsResponse>;
}

export interface ProductServiceClient extends Client {
  /** Creates a product. Returns OK or gRPC error. */
  createProduct(
    request: CreateProductRequest,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  createProduct(
    request: CreateProductRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  createProduct(
    request: CreateProductRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  /** Returns a single product by ID. */
  getProductById(
    request: GetProductByIDRequest,
    callback: (error: ServiceError | null, response: Product) => void,
  ): ClientUnaryCall;
  getProductById(
    request: GetProductByIDRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Product) => void,
  ): ClientUnaryCall;
  getProductById(
    request: GetProductByIDRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Product) => void,
  ): ClientUnaryCall;
  /** Returns all products. */
  getProducts(
    request: Empty,
    callback: (error: ServiceError | null, response: GetProductsResponse) => void,
  ): ClientUnaryCall;
  getProducts(
    request: Empty,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetProductsResponse) => void,
  ): ClientUnaryCall;
  getProducts(
    request: Empty,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetProductsResponse) => void,
  ): ClientUnaryCall;
}

export const ProductServiceClient = makeGenericClientConstructor(
  ProductServiceService,
  "product.ProductService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): ProductServiceClient;
  service: typeof ProductServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
